# Never commit these — secrets, local data, and dependencies
.env
.env.local
*.db
*.db-shm
*.db-wal
node_modules/
server.log
*.log

# Local database backups (scripts/backup.js) contain children's PII —
# these must never end up in git history.
backups/
*.backup.json.gz
